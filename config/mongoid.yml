development:
  sessions:
    default:
      database: codename_one_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: codename_one_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0
